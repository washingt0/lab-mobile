import 'package:flutter/material.dart';

/// display financial-related content
class FinancialPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) => Container(
        child: const Text('Financial'),
      );
}
